export const ejecutar = async () => {
    // const usuarios = await obtenerUsuarios();
    // if (usuarios.length === 0) {
    //     escribirLog("⚠️ No hay usuarios aprobados.");
    //     return;
    // }

    // const driver = await iniciarNavegador();
    // await buscarComunidad(driver);

    // for (const numero of usuarios) {
    //     await agregarUsuario(driver, numero);
    // }

    // escribirLog("🎉 Proceso finalizado.");
    // await driver.quit();
};